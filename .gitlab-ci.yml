build_coek:
  script:
    - apt-get update
    - apt-get install -y g++ cmake git
    - pwd
    - cd ..
    - ls
    - git clone https://gitlab.com:coopr/coek.git
    - cd coek
    - mkdir build
    - cd build
    - cmake -D with_tests=ON ..
    - make
    - make test_verbose
    - cd ../..
