Instructions to build COEK expression api library:

0) Checkout Catch2

   git clone https://github.com/catchorg/Catch2.git catch

1) make a build directory

   mkdir build

2) within the build directory run cmake

   cd build
   cmake -Dwith_tests=ON ..
   make VERBOSE=1

If gurobi is available add -DGUROBI_HOME=<PATH_TO_GUROBI_HOME> and cmake will include it in the compilation

